# ###############################################################
# Loading libraries, setting locale for time;
#
# Should be executed prior to any other scripts;

library(rmarkdown)
library(rjson)
# library(drc)
library(TeachingDemos)
library(maps)
library(rgdal)
library(openxlsx)

Sys.setlocale("LC_TIME", "en_US.UTF-8")
